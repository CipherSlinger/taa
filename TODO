- [x] 任务终止接口
- [x] 任务终端日志
- [x] 任务进度
- [x] 模型下发结果上报、审计结果上报接口拆分（/v1/taa/reportModelImport、/v1/taa/reportAudit）
- [x] 高中低风险统计字段（statistics: high/medium/low）
- [x] 模型下载上报增加 hash 字段（checksum）
- [x] 结果返回使用zip包格式

- 返回结果中移除加入的json结果文件 修改为 接口约束

架构调整：
taa / qwen 独立 + 通信协议
-----------------------------------------------------------------------------------------

TAA 优化：
- taa 重启敏感数据持久化处理：使用sealing key加密保存
- 只支持python 正则匹配，修改为使用 semgrep 扫描

代码审计：
- qwen thinking 模式
- Finding 级并发仲裁（Worker Pool）：
   目前 Go 端是单协程 for 循环逐个验证，若改用 3~5 个 Worker 的并发协程池并发向 Ollama 发送切片推理，12 次请求耗时可直接除以并发度。
- 同文件多 Finding 聚合送审（Batch Prompting）：
   将同一个文件内命中的多处 Finding 合并为一个结构化 Prompt 一次性交由 LLM 判决，把 15 次 HTTP 往返骤降为 2~3 次。
